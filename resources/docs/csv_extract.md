# CSV field extraction guide

1. Nettoyer les données sources avec `action.data.csv.clean`.
2. Exécuter le helper `code.csv.extract_fields` pour ne conserver que les colonnes utiles.
3. Versionner le CSV résultant pour audit.
